{
  "name": "slack-helpmefindmj",
  "description": "HELP ME FIND HIM",
  "repository": "https://github.com/ryanluton/helpmefindmj",
  "website": "https://github.com/ryanluton/helpmefindmj",
  "keywords": ["slack", "command", "bot", "chat"],
  "env": {
    "SLACK_HOOK_URL": {
      "description": "Slack incoming WebHook URL",
      "required": true
    },
    "USERNAME": {
      "description": "Optional: Username to use when replying with the conversion result (default: MJBOT)",
      "required": false
    },
    "EMOJI": {
      "description": "Optional: Emoji icon to use when replying with the conversion result (default: :question:)",
      "required": false
    },
    "SLACK_TOKEN": {
      "description": "Optional: Additional security step: Slack slash command token for verification that the request came from your Slack team",
      "required": false
    }
  }
}
